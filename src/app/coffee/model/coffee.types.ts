export type Priority = 'low' | 'medium' | 'high';
export type DiscussionState = 'todo' | 'in-progress' | 'complete';
export type PostAs = string | 'anonymous' // A bit redundant here as anonymous is a string
